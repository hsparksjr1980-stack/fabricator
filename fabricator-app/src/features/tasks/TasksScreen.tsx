import { Pressable } from 'react-native';import { Card } from '@/components/Card';import { Screen } from '@/components/Screen';import { AppText, Label, Title } from '@/components/Text';import { StatusPill } from '@/components/StatusPill';import { useFabricatorStore } from '@/state/useFabricatorStore';
export function TasksScreen(){const store=useFabricatorStore();return <Screen><Label>Checklist</Label><Title>Tasks</Title>{store.tasks.filter(t=>t.projectId===store.selectedProjectId).map(t=><Pressable key={t.id} onPress={()=>store.cycleTask(t.id)}><Card><Label>{t.system}</Label><Title style={{fontSize:18}}>{t.title}</Title><StatusPill label={t.status}/><AppText style={{marginTop:8}}>Tap card to cycle status.</AppText></Card></Pressable>)}</Screen>}
