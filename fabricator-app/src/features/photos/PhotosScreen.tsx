import { Image } from 'react-native';import { Card } from '@/components/Card';import { Screen } from '@/components/Screen';import { AppText, Label, Title } from '@/components/Text';import { StatusPill } from '@/components/StatusPill';import { useFabricatorStore } from '@/state/useFabricatorStore';import { radius } from '@/theme/theme';
export function PhotosScreen(){const store=useFabricatorStore();return <Screen><Label>Photo gallery</Label><Title>Tagged progress</Title>{store.photos.filter(p=>p.projectId===store.selectedProjectId).map(p=><Card key={p.id}><Image source={{uri:p.uri}} style={{height:190,borderRadius:radius.md,marginBottom:12}}/><StatusPill label={p.tag}/><AppText style={{marginTop:8}}>{p.caption}</AppText></Card>)}</Screen>}
