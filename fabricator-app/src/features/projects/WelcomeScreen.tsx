import { View } from 'react-native';import { NativeStackScreenProps } from '@react-navigation/native-stack';import { Button } from '@/components/Button';import { Card } from '@/components/Card';import { Screen } from '@/components/Screen';import { AppText, Label, Title } from '@/components/Text';import { StatusPill } from '@/components/StatusPill';import { useFabricatorStore } from '@/state/useFabricatorStore';
export function WelcomeScreen({navigation}:NativeStackScreenProps<any>){const {projects,selectProject}=useFabricatorStore();return <Screen><Label>Fabricator MVP</Label><Title>Keep momentum in the shop.</Title><AppText style={{marginVertical:12}}>AI-assisted memory, organization, parts, photos, and next-session planning for builds. Not CAD, ERP, ecommerce, or a forum.</AppText>{projects.map(p=><Card key={p.id}><Label>{p.category}</Label><Title style={{fontSize:22}}>{p.name}</Title><StatusPill label={`${p.phase} • ${p.status}`}/><AppText style={{marginVertical:10}}>{p.progress}% complete — {p.hook}</AppText><Button title="Open project" onPress={()=>{selectProject(p.id);navigation.navigate('Main')}} /></Card>)}<View style={{height:20}}/><Button title="Create project placeholder" variant="ghost" /></Screen>}
