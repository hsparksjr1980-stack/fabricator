import { Pressable } from 'react-native';import { Card } from '@/components/Card';import { Screen } from '@/components/Screen';import { AppText, Label, Title } from '@/components/Text';import { StatusPill } from '@/components/StatusPill';import { useFabricatorStore } from '@/state/useFabricatorStore';
export function PartsScreen(){const store=useFabricatorStore();return <Screen><Label>Parts & materials</Label><Title>Tracker</Title>{store.parts.filter(p=>p.projectId===store.selectedProjectId).map(p=><Pressable key={p.id} onPress={()=>store.cyclePart(p.id)}><Card><Label>{p.system}</Label><Title style={{fontSize:18}}>{p.name}</Title><StatusPill label={p.status}/>{p.vendor?<AppText style={{marginTop:8}}>Vendor: {p.vendor}</AppText>:null}</Card></Pressable>)}</Screen>}
